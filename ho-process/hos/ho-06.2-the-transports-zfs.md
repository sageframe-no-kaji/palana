---
created: 2026-07-03
status: complete
type: ho-document
project: palana
ho: 06.2
kamae: 5
shape: ha
builds-on:
  - kamae-2-palana-system-design
  - kamae-4-palana-ho-overview
  - ho-05-the-plan-engine
  - ho-06.1-the-transports-rsync-and-proxy
---

# ho-06.2 — The Transports: zfs send/receive

Enactment, second half. Lifts `EnactmentError.unsupportedTransport`: the zfs step sequences ho-05 composed—snapshot, send piped to receive, gated cleanups and destroys—enact through the same event stream, against the throwaway pool, never a live host. The proxied zfs pipeline rides the same `SSHPipeline` pump the tar path proved.

**Out of scope:** any queue, any rollback. Mount management—pālana receives datasets, it does not run the host's mount policy.

**Resolves deferred decisions** (from the ho-overview):

- Progress parsing specifics—`zfs send -v` cadence (deferred decision 5, zfs half)

**Carries from ho-06.1:** the event stream, the gate machinery, the pipeline pump, and the note that zfs verification is not a file count.

---

## Phase 1 — Think

### Decision 1 — Verification grows a second shape: the dataset received

`find | wc -l` is the wrong truth for a block stream. The zfs stream is checksummed end to end by zfs itself—a receive that exits clean IS the byte verification—so the gate's question becomes existence: `zfs list -H -o name <child>` on the destination answers it. `VerificationReport` becomes an enum—`counts(source:destination:)` and `datasetReceived(name:exists:)`—with `matched` computed per case. The 06.1 battery updates mechanically; the gate contract does not move.

### Decision 2 — Two composition corrections, recorded here

ho-05's zfs commands meet reality twice. **`zfs receive` gains `-u`:** on Linux, mounting is root's alone regardless of `zfs allow` delegation, so a bare receive by a delegated user lands the dataset and then fails the auto-mount—an exit code that lies about the transfer. `-u` receives without mounting and the plan says so; mounting after is the host's mount policy, not pālana's. **`zfs send` gains `-v`:** progress for the forwarded path comes from send's own stderr cadence—a header line with the estimated size, then per-second byte lines. A `ZfsSendProgress` parser (sibling of the rsync one) reads bytes and computes fraction against the estimate. The proxied path already has the pipeline's byte counter.

### Decision 3 — The Linux delegation wall, named, not papered over

A move's final step destroys the source dataset, and destroying a *mounted* dataset needs an unmount—root's, on Linux. Enactment does not sudo and does not improvise: if the delegated user cannot unmount, the destroy fails typed and visible after a verified transfer—the copy landed, the source stayed, the panel shows exactly which command refused. That is the honest partial state. The fixture exercises the clean path with an unmounted source; the mounted-source reality is recorded for the field-use docs.

### Decision 4 — The pool plays two hosts, delegated

Fixture surgery in `zfs-fixture.sh`: a `zfs-self` alias operator-side (the lima config's Host line learns a second name) and inside the VM (self-keyed, localhost:22), plus `zfs allow` delegation on the pool for the VM user—send, snapshot, receive, create, destroy, hold, mount. Test datasets are throwaway children (`palana/t62-*`), created and torn down per case.

### Discovery (deferred to execution) — send -v's exact lines and delegation's real perimeter

The `-v` format and estimate line harden against the pool's zfs 2.4 live and get recorded. Whatever `zfs allow` genuinely cannot delegate on this Ubuntu lands in Reflect as the field truth.

---

## Phase 2 — Execute

One bounded conversation. Model: `claude-fable-5`.

1. `VerificationReport` becomes the two-shape enum; 06.1 batteries update; zfs verification path in Transports; `unsupportedTransport` lifted.
2. Engine corrections (`-u`, `-v`) with composition battery updates.
3. `ZfsSendProgress` parser, synthetic battery first.
4. Fixture surgery; live integration: a proxied whole-dataset copy and a forwarded whole-dataset move enact end to end—events, progress, dataset-received verification, gated destroys observed, snapshots cleaned.
5. Full rhythm; floor holds; CI green (zfs suites skip without the fixture; the corpus and unit batteries carry the truth everywhere).

### Done means

- A whole-dataset move completes over `zfs send/receive` against the throwaway pool, verified by the dataset's existence, gates enforced.
- The proxied zfs pipeline streams through the operator's machine with bytes counted.
- Send progress parses live on the forwarded path.

---

## Phase 3 — Reflect

**send -v against zfs 2.4.** The header (`estimated size is 12.6K`, a `total estimated size` line for -R) and the column header (`TIME SENT SNAPSHOT`) parse as noise, exactly as designed; the per-second cadence never fired live because fixture-scale sends finish inside a second—the parser's per-second battery is synthetic plus the shapes zfs's own docs commit to. The estimate-anchored fraction and the cap-at-one rule stand ready for real gigabytes. One tool note: a swift-testing quirk (optional-chained `Int64` vs `Int` literal in `#expect` reporting equal values as unequal) cost a debugging loop—bind with `#require` first, compare directly.

**What delegation actually covered.** The Ubuntu perimeter, recorded: `send,snapshot,hold,create,receive,mount,destroy` covered the verbs—and the surprise is that **property names ride along**. Creating with `-o canmount=noauto`, or receiving a `-R` stream that carries that property, fails with a bare permission-denied unless `canmount` itself is delegated. `/dev/zfs` was already world-writable on Ubuntu; the wall was never the device. Mounting stayed root's, as named: content checks in the battery mount fixture-side under sudo, and no command pālana composes ever does.

**The two-shape VerificationReport.** The gate contract held without modification—`matched` computes per shape, the release logic never learned which shape it was reading. The mismatch battery proves both directions: short counts hold file gates, a missing dataset holds zfs gates, and in each case the transcript's silence (no destroy entries to play back) is the proof the gate never leaked.

**Checkpoint 2 readiness.** The engine is complete and headless. Read in this order: the kamae-4 build record (each ho's close entry, one paragraph each), then ho-05's Reflect for the one committed-line correction (rsync cannot proxy—the tar stream is the honest floor), then this ho's delegation findings if the fleet's zfs hosts run delegated. The recommendation is recorded in the build record with the checkpoint entry.

---

_Authored: 2026-07-03 (Think phase). Executed and closed: 2026-07-03._
_173 tests, 34 suites. PalanaCore 97.69% line coverage against the 90% floor._
